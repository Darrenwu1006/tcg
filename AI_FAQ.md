# 常見問題解答（續）

## 新卡適應時間

### Q: 如果未來有新卡加入 pool，模型適應需要多久？

**答案：非常快！幾乎是立即的。**

#### 不同 AI 方案的適應速度

| AI 方案            | 新卡適應時間      | 說明                         |
| ------------------ | ----------------- | ---------------------------- |
| **啟發式 AI**      | **0 秒** ✨       | 規則是通用的，自動適用於新卡 |
| **MCTS**           | **0-5 分鐘**      | 只需要跑幾百局熟悉新卡強度   |
| **RL（強化學習）** | **數小時到 1 天** | 需要重新訓練，但可以增量學習 |
| **LLM 技能解釋**   | **1-2 分鐘**      | 只需解釋新卡技能文字         |

#### 具體流程

##### 步驟 1：新卡加入 Pool（1 分鐘）

```bash
# 你只需要更新 CSV 文件
public/pool/All_Characters.csv  # 添加新角色
public/pool/All_Events.csv      # 添加新事件
```

##### 步驟 2：LLM 解釋新卡技能（1-2 分鐘）

```bash
# 運行技能預處理腳本
npm run process-skills

# 輸出：只處理「新增的」卡片
Processing new cards: 5 張
✓ HV-NEW-001: 新角色 - 已解析
✓ HV-NEW-002: 新角色 - 已解析
...
總計成本: $0.00125 (5 張 × $0.00025)
```

**成本極低**：每張新卡 < $0.001

##### 步驟 3：AI 適應新卡（自動）

**啟發式 AI**：

- 立即可用，無需訓練
- 自動使用新卡的數值做決策

**MCTS**：

```bash
# 可選：讓 MCTS 跑幾百局熟悉新卡
npm run mcts-warmup --cards=5

# 約 5 分鐘，完全免費
```

**RL（如果有用）**：

```python
# 增量學習模式（不用從頭訓練）
model.learn(additional_timesteps=10000)
# 約 2-4 小時
```

#### 實際例子

假設你加入 **5 張新的青葉城西卡片**：

```
時間軸：
00:00 - 你更新 CSV 文件
00:01 - 運行 npm run process-skills
00:02 - LLM 解釋完成（$0.00125）
00:02 - 啟發式 AI 立即可用 ✅
00:07 - MCTS 熟悉完成（跑 500 局）✅
02:00 - RL 增量訓練完成 ✅

總成本：$0.00125
總時間：啟發 AI 立即、MCTS 5分鐘、RL 2小時
```

#### 為什麼這麼快？

1. **LLM 只處理新卡**

   - 已處理的卡片有緩存
   - 只需解釋新增的技能

2. **AI 是通用的**

   - 啟發式規則不依賴特定卡片
   - MCTS 搜索算法通用
   - RL 可以增量學習

3. **組牌測試獨立**
   - 可以立即測試新組牌
   - 不需要重新訓練整個系統

---

## PASS 循環問題

### Q: 「遊戲陷入 PASS 循環」需要先修正嗎？

**答案：不需要！可以往後走。**

#### 問題分析

**發生原因**：

```
發球階段 → 對手選擇防守 →
因為遊戲邏輯還不完整，對手只能 PASS →
又回到對手回合 → 繼續 PASS → 無限循環
```

**為什麼發生**：

- 引擎目前只實現基礎規則
- 某些階段轉換邏輯不完整
- 測試用的「隨機策略」太笨（一直選 PASS）

#### 影響評估

| 影響範圍     | 是否影響  | 說明                         |
| ------------ | --------- | ---------------------------- |
| **核心引擎** | ❌ 不影響 | 狀態管理、動作執行都正常     |
| **AI 訓練**  | ❌ 不影響 | AI 會選擇合理動作，不會 PASS |
| **組牌測試** | ❌ 不影響 | 真實對局不會出現此問題       |
| **MCTS**     | ❌ 不影響 | MCTS 會探索所有動作          |
| **測試套件** | ✅ 影響   | Test 4 無法完整跑完          |

#### 為什麼可以先不修？

**原因 1：AI 不會這樣玩**

```typescript
// 測試用的「笨」隨機策略
const randomAction = actions[Math.floor(Math.random() * actions.length)];
// 可能一直選到 PASS

// 真實 AI（啟發式）
const bestAction = chooseSmartAction(state);
// 會選擇發球、攔網等合理動作，不會 PASS
```

**原因 2：問題是測試方法，非引擎本身**

- 引擎邏輯正確
- 只是測試的「完全隨機」策略太蠢
- 真實使用時不會出現

**原因 3：修正成本 vs 收益**

- 修正時間：1-2 小時
- 收益：只是讓 Test 4 完美通過
- 不如把時間用在 Phase 3（LLM 技能處理）

#### 何時修正？

建議在以下時機修正：

1. **Phase 3 完成後**
   - 技能系統整合時，會自然修正階段轉換
2. **發現真實問題時**
   - 如果 AI 訓練時出現類似問題
3. **有空閒時間時**
   - 作為低優先級的優化項目

#### 臨時解決方案（如果真的想測試）

可以加一個簡單的檢查：

```typescript
// 在 simulateToEnd 中
if (consecutivePassCount > 10) {
  // 強制執行一個非 PASS 動作
  const nonPassActions = actions.filter((a) => a.type !== "PASS");
  if (nonPassActions.length > 0) {
    action = nonPassActions[0];
  }
}
```

但這不是必要的。

---

## LLM API 費用控制

### Q: LLM 需要 API 嗎？如何避免意外扣款？

**答案：推薦用 Ollama（完全免費），如需 API 有多重保護。**

#### 方案對比

| 方案               | 費用          | 優點               | 缺點            |
| ------------------ | ------------- | ------------------ | --------------- |
| **Ollama（推薦）** | **$0**        | 完全免費、無限使用 | 需要 8GB+ RAM   |
| **Claude API**     | ~$0.03 一次性 | 質量最高           | 需網路、API key |
| **OpenAI API**     | ~$0.10 一次性 | 質量高             | 需網路、API key |

#### 推薦方案：Ollama（零成本）✅

**為什麼推薦 Ollama**：

1. **完全免費** - 在你電腦上運行，無 API 費用
2. **無限使用** - 想處理多少卡片都可以
3. **隱私安全** - 數據不離開你的電腦
4. **無需網路** - 本地運行

**安裝步驟**：

```bash
# 1. 安裝 Ollama（一次性）
brew install ollama

# 2. 下載模型（一次性，約 2GB）
ollama pull llama3.2:3b

# 3. 使用（完全免費）
# 運行技能處理腳本，自動使用本地 LLM
npm run process-skills
```

**你的電腦可以嗎？**

- Mac（你在用）：✅ 完全沒問題
- RAM：建議 8GB+（你應該有）
- 空間：需要約 4GB

#### 備選方案：API（如需更高質量）

如果你想用 Claude/GPT 獲得更好的技能解析質量：

##### 保護措施 1：預處理模式（推薦）

```bash
# 一次性處理所有卡片
npm run process-skills-once

# 會顯示：
⚠️  WARNING: 將使用 Claude API
預估費用: $0.03 (126 張卡)
確認繼續? (y/n)
```

**特點**：

- 處理完就存檔，之後**零成本**
- 新卡才需要再處理（每張 < $0.001）

##### 保護措施 2：費用上限

```typescript
// 配置文件中設定
const MAX_API_COST = 0.1; // 最多 $0.10

// 運行時檢查
if (estimatedCost > MAX_API_COST) {
  throw new Error("超過費用上限！");
}
```

##### 保護措施 3：本地緩存

```json
// public/skills/interpreted_skills.json
{
  "HV-P01-001": { /* 已解析 */ },
  "HV-P01-002": { /* 已解析 */ },
  ...
}
```

**只處理新卡**：

- 已處理的卡片：讀取緩存（$0）
- 新卡：才調用 API（< $0.001/張）

##### 保護措施 4：詳細計費記錄

```bash
# 運行後顯示
✓ 處理完成
  - 已緩存: 121 張（$0）
  - 新處理: 5 張（$0.00125）
  - 總計: $0.00125

記錄已保存到: logs/api_usage.json
```

#### 推薦流程

**第一次使用**：

```bash
# 用 Ollama（免費）
ollama pull llama3.2:3b
npm run process-skills

# 如果質量不滿意，再考慮用 Claude
npm run process-skills --provider=claude
```

**之後新卡**：

```bash
# 只處理新卡，成本極低
npm run process-skills
# 5 張新卡 = $0.00125
```

#### 費用總結

| 情境                  | Ollama | Claude API |
| --------------------- | ------ | ---------- |
| **首次處理 126 張卡** | $0     | $0.03      |
| **每次新增 5 張卡**   | $0     | $0.00125   |
| **一年新增 100 張卡** | $0     | $0.025     |
| **總成本（3 年）**    | **$0** | **~$0.11** |

**結論**：無論哪種方案，成本都極低！

---

## 你的青葉城西牌組

我看到你有：

- `中速軸.csv`
- `快攻軸.csv`

想用這些測試勝率嗎？我可以：

1. 創建一個測試腳本
2. 讓兩副牌互相對戰 1000 局
3. 統計勝率

這會幫助我們驗證引擎的完整性！要試試看嗎？
